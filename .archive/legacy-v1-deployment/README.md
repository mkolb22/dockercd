# Inactive V1-predecessor deployment material

This directory preserves retired embedded-controller UI assets, bundle and
cluster Compose overlays, deployment manifests, and installer material for
historical reference only. Nothing under `.archive/legacy-v1-deployment` is
loaded, built, deployed, or supported by the v1 release path. Do not copy an
archived file into an active deployment without a new design review and
explicit migration plan.
